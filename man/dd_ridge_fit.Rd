% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poly_fit.R
\name{dd_ridge_fit}
\alias{dd_ridge_fit}
\title{Ridge regression (closed form)}
\usage{
dd_ridge_fit(X, y, alpha = 0, weights = NULL)
}
\arguments{
\item{X}{Numeric design matrix (n x p).}

\item{y}{Numeric response vector (length n).}

\item{alpha}{Numeric >= 0, ridge penalty.}

\item{weights}{Optional numeric vector of sample weights (length n).}
}
\value{
Numeric coefficient vector (length p).
}
\description{
Equivalent to scikit-learn's \code{sklearn.linear_model.ridge_regression} with
default settings: no intercept centering (an intercept column must be
included in \code{X} explicitly, as the polynomial dictionaries here do), and
an optional \code{alpha} L2 penalty applied uniformly to every column,
including that intercept column, exactly as PyDaddy relies on.
}
\keyword{internal}
