% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{dd_auto_dt}
\alias{dd_auto_dt}
\title{Auto-select a drift/diffusion timescale (Dt) from the autocorrelation time}
\usage{
dd_auto_dt(x)
}
\arguments{
\item{x}{Numeric vector (the time series, or its squared magnitude for
vector data).}
}
\description{
Auto-select a drift/diffusion timescale (Dt) from the autocorrelation time
}
\section{Deviation from PyDaddy}{
PyDaddy's internal \code{Main} class computes exactly this (\code{Dt = ceiling(autocorrelation_time / 10)}) when \code{Dt} is left unset — but the
public, documented entry point (\code{pydaddy.Characterize()}) hardcodes
\code{Dt = 1} as its default, so in practice this auto-selection is unreachable
from the documented API. Since the auto-selection logic clearly exists and
is exercised elsewhere in the code (the interactive timescale-slider
feature), daddyR treats it as the intended default: \code{dd_analyse()} uses
\code{Dt = NULL} (meaning "auto") by default. Pass an explicit \code{Dt} to opt out.
}

\keyword{internal}
