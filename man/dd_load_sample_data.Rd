% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{dd_load_sample_data}
\alias{dd_load_sample_data}
\title{Load a bundled example dataset}
\usage{
dd_load_sample_data(name = c("scalar-pairwise", "scalar-ternary"))
}
\arguments{
\item{name}{One of \code{"scalar-pairwise"} or \code{"scalar-ternary"}: simulated
time series from simple pairwise- and ternary-interaction models
(Jhawar & Guttal 2020), used throughout the PyDaddy paper's synthetic
examples.}
}
\value{
A list with \code{x} (the time series) and \code{t_int} (the time interval
between observations), ready to pass to \code{\link[=dd_analyse]{dd_analyse()}} as
\code{dd_analyse(out$x, t = out$t_int)}.
}
\description{
Loads one of the small time series datasets shipped with daddyR for
examples and testing, mirroring PyDaddy's \code{load_sample_dataset()}.
Currently only scalar (1D) datasets are bundled; vector datasets (e.g.
the fish-schooling and cell-migration data from the original paper) will
be added once vector-data support is ported (see the package roadmap).
}
\examples{
d <- dd_load_sample_data("scalar-pairwise")
str(d)
}
