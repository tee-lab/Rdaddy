% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{dd_estimate}
\alias{dd_estimate}
\title{Kramers-Moyal drift and diffusion estimation}
\usage{
dd_estimate(
  x,
  t_int,
  Dt = 1L,
  dt = 1L,
  bins = 20L,
  inc = NULL,
  op_range = NULL
)
}
\arguments{
\item{x}{Numeric vector, the (scalar) time series.}

\item{t_int}{Numeric scalar, time interval between observations.}

\item{Dt}{Integer, lag (in samples) used for the drift estimate.}

\item{dt}{Integer, lag (in samples) used for the diffusion estimate.}

\item{bins}{Integer, number of bins to use across the range of \code{x}
(ignored if \code{inc} is supplied).}

\item{inc}{Numeric, explicit bin width to use instead of \code{bins}.}

\item{op_range}{Optional length-2 numeric vector giving the range over
which to bin; defaults to \code{range(x, na.rm = TRUE)}.}
}
\value{
A list with components:
\item{op}{Bin-center order-parameter values (length \code{bins}).}
\item{avg_drift, avg_diff}{Binned average drift/diffusion at each \code{op}.}
\item{drift_ebar, diff_ebar}{Standard errors of the binned averages.}
\item{drift_n, diff_n}{Number of points contributing to each bin.}
\item{drift_series, diff_series}{The full (unbinned) drift/diffusion
series, aligned to \code{x_for_fit}.}
\item{x_for_fit}{The \code{x} values (\code{x[1:(n - Dt)]}) that
\code{drift_series} and \code{diff_series} are indexed against; use this,
together with \code{drift_series}/\code{diff_series}, as the regression data for
\code{\link[=dd_fit_polynomial]{dd_fit_polynomial()}}.}
}
\description{
Core numerical machinery for estimating drift and diffusion coefficients
from a scalar time series, following the Kramers-Moyal / Friedrich-Peinke
approach used throughout PyDaddy. Given a time series \code{x} sampled at
interval \code{t_int}, the drift at lag \code{Dt} is the finite-difference estimate
\code{(x[t+Dt] - x[t]) / (t_int * Dt)}, binned by the value of \code{x[t]}; the
diffusion is estimated from the squared residual of \code{x} after removing the
locally-averaged drift.
}
\section{Deviation from PyDaddy: consistent bin edges}{
PyDaddy computes the bin width for grouping data (\code{inc}) as
\code{(max - min) / bins}, but separately reports the bin locations (\code{op}) as
\code{numpy.linspace(min, max, bins)}, whose spacing is \code{(max - min) / (bins - 1)}. These two are different whenever \code{bins > 1}, which means the bins
used to group data internally do not exactly tile the reported \code{op} axis:
depending on rounding, this leaves narrow gaps between consecutive bins
(points silently excluded from every bin) or slight overlaps. daddyR fixes
this by constructing \code{bins + 1} edges that exactly span the data range,
using bin midpoints as the reported order-parameter values. Coverage is
therefore exact and every point falls in exactly one bin. This means
\code{dd_estimate()}'s binned averages will not match PyDaddy's bit-for-bit
(see \code{DESIGN_DECISIONS.md}), but the underlying unbinned drift/diffusion
series used for polynomial fitting are unaffected and match exactly.
}

\keyword{internal}
