% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse.R
\name{dd_fit}
\alias{dd_fit}
\title{Fit a polynomial to the drift or diffusion function}
\usage{
dd_fit(
  dd,
  which = c("drift", "diffusion"),
  degree = 3,
  threshold = 0,
  alpha = 0,
  tune = FALSE,
  ...
)
}
\arguments{
\item{dd}{A \code{"daddy"} object.}

\item{which}{One of \code{"drift"} or \code{"diffusion"}.}

\item{degree}{Maximum polynomial degree.}

\item{threshold}{Sparsity threshold. Ignored if \code{tune = TRUE}.}

\item{alpha}{Ridge regularization strength.}

\item{tune}{If \code{TRUE}, choose \code{threshold} automatically via
cross-validation (see \code{\link[=dd_tune_threshold]{dd_tune_threshold()}}).}

\item{...}{Additional arguments passed to \code{\link[=dd_tune_threshold]{dd_tune_threshold()}} when
\code{tune = TRUE}.}
}
\value{
The \code{dd} object, with \code{dd$fits$drift} or \code{dd$fits$diffusion} set
to the fitted \code{poly1d}.
}
\description{
Fits a sparse polynomial to the \emph{unbinned} drift or diffusion series
(i.e. to every data point, not to the binned averages), matching
PyDaddy's \code{Daddy.fit()}. Unlike PyDaddy, this does not mutate \code{dd} in
place (R objects are not reference objects); it returns an updated copy,
which is the idiomatic R pattern for this kind of "add a computed result"
step: \code{dd <- dd_fit(dd, "drift", degree = 3)}.
}
