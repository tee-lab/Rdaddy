% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse.R
\name{dd_analyse}
\alias{dd_analyse}
\title{Analyse a scalar time series as a stochastic differential equation}
\usage{
dd_analyse(
  data,
  t = 1,
  Dt = NULL,
  dt = Dt,
  bins = 20L,
  inc = NULL,
  op_range = NULL
)
}
\arguments{
\item{data}{Numeric vector: the time series to analyse.}

\item{t}{Either a single positive number (the time interval between
observations) or a numeric vector of timestamps the same length as
\code{data}.}

\item{Dt}{Integer lag for the drift estimate, or \code{NULL} (default) to
auto-select from the autocorrelation time.}

\item{dt}{Integer lag for the diffusion estimate. Defaults to \code{Dt} (unlike
PyDaddy, which defaults \code{dt} to a fixed \code{1} independent of \code{Dt}; letting
diffusion and drift share a timescale by default avoids the
index-alignment inconsistencies documented in \code{DESIGN_DECISIONS.md}
that arise in PyDaddy's own code whenever \code{Dt != dt}).}

\item{bins}{Integer number of bins across the data range, or the string
\code{"auto"} to choose via the Freedman-Diaconis rule. Default \code{20}.}

\item{inc}{Optional explicit bin width, used instead of \code{bins}.}

\item{op_range}{Optional length-2 vector giving the order-parameter range
to bin over; defaults to the observed data range.}
}
\value{
An object of class \code{"daddy"}.
}
\description{
Estimates binned drift and diffusion (Kramers-Moyal) coefficients from a
scalar time series \code{x}, following the method of Nabeel et al. (2025). This
is the main entry point of the package; the result is an object of class
\code{"daddy"} that \code{\link[=dd_drift]{dd_drift()}}, \code{\link[=dd_diffusion]{dd_diffusion()}}, \code{\link[=dd_fit]{dd_fit()}},
\code{\link[=dd_export_data]{dd_export_data()}}, and the \code{print}/\code{summary}/\code{plot} methods all operate
on.
}
\section{Deviations from PyDaddy's \code{Characterize()}}{
\itemize{
\item \code{Dt} defaults to \code{NULL}, meaning "auto-select from the autocorrelation
time" (\code{ceiling(autocorr_time / 10)}). PyDaddy's equivalent function,
\code{pydaddy.Characterize()}, hardcodes a default of \code{Dt = 1} even though its
own internal auto-selection logic (reachable only through a different,
undocumented code path) computes exactly this quantity. See
\code{DESIGN_DECISIONS.md}.
\item All parameters are named explicitly; there is no catch-all \code{...} that
silently absorbs unrecognized arguments into internal state (as
PyDaddy's constructors do via \code{self.__dict__.update(kwargs)}). A
misspelled argument name here is a clean error, not a silently-ignored
typo.
\item No \code{show_summary} argument — this is a pure function that does not
print or plot as a side effect. Call \code{print()}, \code{summary()}, or \code{plot()}
on the result explicitly.
}
}

\examples{
d <- dd_load_sample_data("scalar-pairwise")
dd <- dd_analyse(d$x, t = d$t_int)
print(dd)
dd_drift(dd)
}
